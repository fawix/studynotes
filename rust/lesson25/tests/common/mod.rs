//This way if we have a utils or common
//file in a shared folder we do not have 
//it generate a separate crate.


fn setup ( ) {
    //do some test setup common
    //to many integration tests modules
}